const printGreetings = (message = 'Mensagem Padrão') => {
  /* 
    Caso não passe nenhum valor
    Quando chamar a funcao
    Vai receber o valor do parametro
  */
  console.log(message);
}

printGreetings('Seja bem vindo(a)');